// Tailwind CSS v4 — PostCSS plugin only (no tailwind.config.js needed; theme
// lives in app/globals.css via @theme directive).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
